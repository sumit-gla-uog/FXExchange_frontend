import { Card, H3, Text } from "@salt-ds/core";

export const DashboardPage = () => {
  return (
    <Card style={{ padding: 16 }}>
      <H3>Dashboard</H3>
      <Text styleAs="secondary">We will build the full dashboard UI here next.</Text>
    </Card>
  );
}